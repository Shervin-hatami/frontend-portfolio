/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost', 
      'backend-portfolio-app.onrender.com',
      'res.cloudinary.com'
    ],
  },
}

module.exports = nextConfig 